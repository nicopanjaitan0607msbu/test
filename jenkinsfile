def name = "local"

podTemplate(
    inheritFrom: 'ubuntu-agent-pod',
    containers: [
        containerTemplate(name: 'ubuntu', image: 'ubuntu:latest', command: 'cat', ttyEnabled: true)
    ]
) {
    node() {
            stage('Test'){
              container('ubuntu') {
              sh "echo 'ini jenkins pod template'"
          }
      }
}
}
